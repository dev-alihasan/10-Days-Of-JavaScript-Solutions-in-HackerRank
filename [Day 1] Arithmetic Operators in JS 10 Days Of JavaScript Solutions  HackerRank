retangle's area

function getArea(length, width) {
  let area;
  area = length * width;
  return area;
}



Perimeter of retangle's

function getPerimeter(length, width) {
  let perimeter;
  perimeter = 2 * (length + width);
  return perimeter;
}
